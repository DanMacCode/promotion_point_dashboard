# Ignore raw data files (only keep the final master dataset)
data/pdfs/
data/txt/
data/csv/
GITHUB_TOKEN.env
# Keep only the master dataset
!data/master/
!data/master/master_promotion_data.csv

# Ignore virtual environments
.venv/
__pycache__/
*.log  # Ignore log files
*.tmp  # Ignore temporary files
.DS_Store  # Ignore macOS system files
Thumbs.db  # Ignore Windows system files

# Ignore environment variables and secrets (if you ever add them)
.env
.vercel
